# Fundamentals

docker --version                            # Complete Version
docker compose version                      # Compose tool version
docker run docker.io/library/hello-world    # Run from registry/library/image
docker run hello-world                      # Run by searching default registry
docker ps                                   # list running container
docker ps -a                                # list all docker containers
docker images                               # list all docker images stored locally

